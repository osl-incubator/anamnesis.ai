name: anamnesisai
channels:
  - nodefaults
  - conda-forge
dependencies:
  - python
  - pip
  - poetry
  - nodejs # used by semantic-release
  - shellcheck
  - srsly 2.4.8
  - pip:
      # distlib issue
      - paginate
      # pip wheel --no-cache-dir --use-pep517 "srsly (==2.4.8)"
